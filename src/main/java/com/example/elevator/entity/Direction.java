package com.example.elevator.entity;

/**
 * Direction of travel for an elevator or a passenger's requested trip.
 * NONE means the elevator is idle / has no pending destination.
 */
public enum Direction {
    UP, DOWN, NONE
}
